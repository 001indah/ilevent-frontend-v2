/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.pexels.com', 'images.unsplash.com', 'unsplash.com', 'source.unsplash.com'],
    },
}


export default nextConfig;
